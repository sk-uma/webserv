server {
  root /var/www/html;

  location /contents {
    root /var/www/contents;
  }

  location /contents/text {
    root /etc/text;
  }

  location /var {
    root /var/www/var;
  }

  location /log/ {
    root   /etc/log;
  }
}
