<reference>
<description>Get status of named file</description>
<shortname>STAT</shortname>
<syntax>
int stat(const char *filename, struct stat *data);
<reference>
<description>Get status of named file</description>
<shortname>STAT</shortname>
<syntax>
int stat(const char *filename, struct stat *data);
...
retval = stat(filename, data);</syntax>
<description><para>
Writes the fields of a system data structure into the
structure pointed to by data.
These fields contain (among other
things) information about the file's access
privileges, owner, and time of last modification.
</para></description>
<parameters>
<param><name>filename</name>
<para>A character string variable or constant containing
the Unix pathname for the file whose status you want:v
to retrieve.
You can give the ...
</para></param></parameters>
</reference>
HELP}aaaa

#include <stdio.h>
printf

wri
42_Piscine/Shell00/
Writes the fields of a system data structure into the
int stat(const char *filename, struct stat *data);
<syntax>aijgai
help
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:
   apri(aba)gagajgja()jgaijiobja()gajijba()(aba()iiiiigiajig;aej() )Z
<reference>
<description>Get status of named file</description>
<shortname>STAT</shortname>
<syntax>
int stat(const char *filename, struct stat *data);
...inni in
retval = stat(filename, data);</syntax>
<description><para>
Writes the fields of a system data structure into the
structure pointed to by data.
These fields contain (among other
things) information about the file's access
privileges, owner, and time of last modification.
</para></description>
<parameters>
<param><name>filename</name>
<para>A character string variable or constant containing
the Unix pathname for the file whose status you want:v
to retrieve.
You can give the ...
</para></param></parameters>
</reference>
HELP}aaaa


    ii		
help
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:
   apri(aba)gagajgja()jgaijiobja()gajijba()(aba()iiiiigiajig;aej() )
...
retval = stat(filename, data);</syntax>
<description><para>
Writes the fields of a system data structure into the
structure pointed to by data.
These fields contain (among other
things) information about the file's access
privileges, owner, and time of last modification.
</para></description>
<parameters>
<reference>
<description>Get status of named file</description>
<shortname>STAT</shortname>
<syntax>
int stat(const char *filename, struct stat *data);
...
retval = stat(filename, data);</syntax>
<description><para>
Writes the fields of a system data structure into the
structure pointed to by data.
These fields contain (among other
things) information about the file's access
privileges, owner, and time of last modification.
</para></description>
<parameters>
<param><name>filename</name>
<para>A character string variable or constant containing
the Unix pathname for the file whose status you want:v
to retrieve.
You can give the ...
</para></param></parameters>
</reference>
HELP}aaaa


    ii		
help
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:
   apri(aba)gagajgja()jgaijiobja()gajijba()(aba()iiiiigiajig;aej() )
<param><name>filename</name>
<para>A character string variable or constant containing
the Unix pathname for the file whose status you want:v
to retrieve.
You can give the ...
</para></param></parameters>
</reference>
HELP}aaaa


    ii		
help
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa:
   apri(aba)gagajgja()jgaijiobja()gajijba()(aba()iiiiigiajig;aej() )


